<?xml version='1.0' encoding='utf-8'?>
<Container version="2">
  <Name>todo</Name>
  <Repository>ubermetroid/todo:latest</Repository>
  <Registry>https://hub.docker.com/r/ubermetroid/todo</Registry>
  <Shell>/bin/sh</Shell>
  <Network>bridge</Network>
  <Privileged>false</Privileged>
  <ExtraParams>--user 99:100 --restart unless-stopped --log-opt max-size=50m</ExtraParams>
  <Support>https://github.com/UberMetroid/todo/issues</Support>
  <Project>https://github.com/UberMetroid/todo</Project>
  <Overview>
    Todo is a clean, secure, and minimalist todo list application built in Rust with Yew and Axum.

    Key Features:
    - High performance and tiny resource footprint.
    - Dynamic theme options.
    - Multilingual interface support.
    - Optional access PIN encryption for privacy.
  </Overview>
  <Category>Productivity:Tools;Tools:Utilities;</Category>
  <WebUI>http://[IP]:[PORT:4403]/</WebUI>
  <Icon>https://raw.githubusercontent.com/UberMetroid/unraid-templates/main/icons/todo.png</Icon>
  <TemplateURL>https://raw.githubusercontent.com/UberMetroid/unraid-templates/main/templates/todo.xml</TemplateURL>
  <Requires>If you lock the application with a PIN, it is recommended to set a strong PIN and back up your data directory. Note: The container runs as nobody:users (UID 99 / GID 100) out of the box. Ensure the mapped host appdata folder has read/write permissions for UID 99 / GID 100.</Requires>
  <Config Name="WebUI Port" Target="4403" Default="4403" Mode="tcp" Description="The host port mapped to the web server (default container port: 4403)." Type="Port" Display="always" Required="true" Mask="false">4403</Config>
  <Config Name="Access PIN" Target="TODO_PIN" Default="" Description="Optional numerical PIN to lock access to the interface. (Optional, not required. Leave empty for unlocked public mode.)" Type="Variable" Display="always" Required="false" Mask="true" />
  <Config Name="App Data" Target="/app/data" Default="/mnt/user/appdata/todo" Mode="rw" Description="Persistent storage for task database files." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/todo</Config>
</Container>